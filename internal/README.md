# ETH Blockchain Parser Consumer

It's a simple consumer of the `parser` package.

Starts an application exposing an HTTP server to:
- subscribe to new transactions
- get transactions
- get latest parsed block
